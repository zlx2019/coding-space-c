/**
 * 在一些高级语言里，通常都会有一种叫string的字符串类型，但是C语言中，并没有提供该类型，
 * 而是使用最原生的方式来表示字符串;
 *
 * 什么是字符串？
 *   所谓'串'也就将多个物体串联在一起，而形成的一种序列;
 *   字符串，也就是由n多个字符(char)所组成的一个字符数组;
 * 例如有'a','b','c'三个字符，将这三个字符串在一起，则就形成了字符串"abc";
 *
 * 在C语言中，字符使用单引号('')包裹，而字符串则使用双引号("")包裹;
 *
 */
