/**
 * 在计算机世界中，一切的一切都由数字所组成，并且是以二进制形式，全都是由0和1两种数字所存储;
 * 所以0和1，则是计算机中的最小存储单元，又称之为 bit(位);
 * (0 || 1) = 1bit
 *
 * 1byte = 8bit
 * word = (2byte | 4byte | 8byte)
 *  word(字)是CPU能够一次读取或存储的最大单位，不同的CPU架构word大小也不同，32位系统字长为4byte,64位系统字长为8byte;
 * dword = 2word (double word)
 * qword = 8byte
 *
 * 1KB = 1024byte
 * 1MB = 1024KB
 * 1GB = 1024MB
 * 1TB = 1024GB
 * 1PB = 1024TB
 * 1EB = 1024PB
 * 1ZB = 1024EB
 * 1YB = 1024ZB
 */